<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.notifications_demo"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>

        <!-- Declare our background silent-push handler on the plugin's FCM
             service. `tools:node="merge"` adds this meta-data to the service
             node contributed by the plugin manifest instead of redefining it.
             The handler runs even when the app is killed (cold start). -->
        <service
            android:name="app.tauri.notification.TauriFirebaseMessagingService"
            tools:node="merge">
            <meta-data
                android:name="app.tauri.notification.SILENT_PUSH_HANDLER"
                android:value="com.test.app.DemoSilentPushHandler" />
        </service>

        <!-- Debug-only: lets `adb am broadcast` synthesize a silent push so the
             killed-state path is testable without a Firebase backend. Exported
             so adb can reach it; remove for production. -->
        <receiver
            android:name=".DebugSilentPushReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.test.app.DEBUG_SILENT_PUSH" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
