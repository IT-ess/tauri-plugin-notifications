<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- AndroidTV support -->
    <uses-feature android:name="android.software.leanback" android:required="false" />

    <application
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.notifications_demo"
        android:usesCleartextTraffic="${usesCleartextTraffic}">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:label="@string/main_activity_title"
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- AndroidTV support -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

            <!-- Matrix deep links (Option B): a notification tap fires
                 ACTION_VIEW for a `matrix:` URI, which tauri-plugin-deep-link
                 surfaces to JS via onOpenUrl. Custom-scheme filters aren't
                 auto-generated by the deep-link plugin, so declare it here. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="matrix" />
            </intent-filter>
        </activity>

        <provider
          android:name="androidx.core.content.FileProvider"
          android:authorities="${applicationId}.fileprovider"
          android:exported="false"
          android:grantUriPermissions="true">
          <meta-data
            android:name="android.support.FILE_PROVIDER_PATHS"
            android:resource="@xml/file_paths" />
        </provider>

        <!-- Name the app's native library on the plugin's FCM service so
             data-only pushes reach the Rust handler registered with
             `silent_push_handler!` — even when the app is killed (cold start).
             The value is the cargo `[lib]` name (what System.loadLibrary
             takes). `tools:node="merge"` adds this meta-data to the service
             node contributed by the plugin manifest. -->
        <service
            android:name="app.tauri.notification.TauriFirebaseMessagingService"
            tools:node="merge">
            <meta-data
                android:name="app.tauri.notification.SILENT_PUSH_LIB"
                android:value="notifications_demo_lib" />
        </service>
    </application>
</manifest>
